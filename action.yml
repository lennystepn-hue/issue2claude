name: 'Issue2Claude'
description: 'Automatically solve GitHub Issues with Claude Code'
author: 'issue2claude'

inputs:
  anthropic-api-key:
    description: 'Anthropic API Key (required for api-key mode)'
    required: false
    default: ''
  github-token:
    description: 'GitHub Token'
    required: true
  auth-mode:
    description: 'Authentication mode: "api-key" or "max" (Claude Max/Pro subscription)'
    required: false
    default: 'api-key'
  oauth-token:
    description: 'Claude OAuth token from ~/.claude/.credentials.json (required for max mode)'
    required: false
    default: ''
  issue-number:
    description: 'Issue number to solve'
    required: true
  issue-title:
    description: 'Issue title'
    required: true
  issue-body:
    description: 'Issue body/description'
    required: true
  repo:
    description: 'Repository (owner/name)'
    required: true
  model:
    description: 'Claude model to use'
    default: 'claude-sonnet-4-6'

runs:
  using: 'node20'
  main: 'action/index.js'
